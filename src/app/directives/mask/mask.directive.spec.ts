import { AppMaskDirective } from './app-mask.directive';

describe('AppMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMaskDirective();
    expect(directive).toBeTruthy();
  });
});
