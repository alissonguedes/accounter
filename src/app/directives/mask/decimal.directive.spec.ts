import { MoedaDirective } from './moeda.directive';

describe('MoedaDirective', () => {
  it('should create an instance', () => {
    const directive = new MoedaDirective();
    expect(directive).toBeTruthy();
  });
});
